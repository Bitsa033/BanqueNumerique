{% extends "base.html.twig" %}
{% block body %}
<section class="content-wrapper">

    <div class="row">
        {% for message in app.flashes('statut_off') %}
        <div class="alert alert-danger container text-center col-md-10 offset-1" style="margin-top: 2%;">
            {{ message }}
        </div>
        {% endfor %}

        {% for message in app.flashes('success') %}
        <div class="alert alert-success text-center col-md-10 offset-1">
            {{ message }}
        </div>
        {% endfor %}
        <div class="col-md-10 offset-1">
            <form autocomplete="off" id="form" method="post" action="{{path('crediterCompteB')}}">
                <div class="form-group">
                    <label for="id_depot">Numero de Compte</label>
                    <input type="text" name="id_compte" class="form-control" id="id_compte" required>
                    <div class="validate"></div>
                </div>
                <div class="form-group">
                    <label for="somme">Entrer le montant</label>
                    <input type="text" name="montant" class="form-control" id="montant" required>
                    <div class="validate"></div>
                </div>

                <button type="submit" class="btn btn-primary offset-2">Créditer le compte</button>

            </form>

        </div>
    </div>


</section>
{% endblock body %}